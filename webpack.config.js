const path = require('path');

module.exports = {
  entry: '',
  output: {
    path: '',
    filename: 'main.js'
  },
  
  module: {
    rules: [
      {
        test: '',
        exclude: /node_modules/,
        loader: '',
        query: {
          presets: [],
        },
      },
    ],
  }
}

