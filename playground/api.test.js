
import express from 'express';
const app = express;

/*describe('Home page route GET /', () => {
   /* it('should GET /', (done) => {
    request(app)
      .get('/')
      .expect(200)
      .end((err, res) => {
        if(err) return done(err);
        done();
      });
  }); // end it
});*/ //end Home page